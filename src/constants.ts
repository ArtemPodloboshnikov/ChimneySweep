export enum loaderType {
    MESSAGE,
    BUTTON
}

export const ids = {
    FILE_CHECK_LOADER: 'filesCheck',
    FOLDER_LOADER: 'loadFolder'
}

export enum FileInfoType {
    DANGER='danger',
    ACTIVE='active',
    ATTENTION='attention'
}

export enum BrowserType {
    DEPENDENT,
    FOLDER
}